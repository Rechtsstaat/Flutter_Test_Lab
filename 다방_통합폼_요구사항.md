# 다방이 통합 폼에 요구하는 것

실물 다방프로(`pro.dabangapp.com/form/room`)에 실제 중개사 계정으로 붙여 재 본 결과,
**통합 폼에 값이 없어서** 채우지 못한 항목들을 정리한 문서입니다. 통합 폼 변경은 직방·당근까지
한꺼번에 볼 때 하기로 하고, 여기에는 **다방 쪽 사실관계만** 남깁니다.

작성: 2026-09-20. 다른 세션에서 이 문서만 읽고 작업할 수 있게 쓴 것이므로, 손대기 전에
「[다시 재는 법](#다시-재는-법)」으로 실물이 그대로인지 한 번 확인하세요.

---

## 이 문서의 근거

| 무엇 | 어떻게 | 신뢰도 |
|---|---|---|
| 다방 폼의 칸·선택지 | 실물 로그인 상태에서 `test/tools/dabang_form_probe.js` 로 채집 (2026-09-20) | **실측**. 미러와 한 글자도 다르지 않았음 (`test/tools/dabang_form_probe.mirror.json`) |
| 통합 폼의 현재 칸 | `lib/fields.dart` 의 `MasterField` · `HifiField` | 코드 |
| 「무엇을 못 채웠는가」 | 어댑터가 실물에서 낸 「확인할 항목 / 참고 사항」 (실기기, 2026-09-20) | **실측** |

**주의**: 다방 폼은 **앞 선택에 따라 행이 다시 그려집니다.** 거래 종류를 고르면 「가격 정보」
행이 그 종류의 틀로 새로 그려지고, 매물유형 대분류를 바꾸면 7개 행이 통째로 다시 그려집니다.
위 채집은 **아무것도 고르지 않은 처음 상태**라, 거래 종류별로만 나타나는 칸(단기 관련 등)은
담기지 않았습니다. 그 부분은 아래에 따로 표시했습니다.

---

## 분류

- **[A] 통합 폼에 칸이 필요함** — 다방은 받는데 우리가 물어보지 않아서 못 넣는 것. **이 문서의 본론.**
- **[B] 코드만 고치면 됨** — 통합 폼에 값이 이미 있는데 어댑터가 안 쓰는 것. 통합 폼 변경 불필요.
- **[C] 다방이 못 받음** — 통합 폼을 바꿔도 소용없는 것. 안내 문구로만 남김. **손대지 말 것.**

---

## [A] 통합 폼에 칸이 필요한 것

### A-1. 에어컨 종류 — 벽걸이형 / 스탠드형 / 천장형

| | |
|---|---|
| **다방 컨트롤** | `facility_info` / **「냉방 시설」** 행. 라벨 세 개: `벽걸이형` · `스탠드형` · `천장형` (체크박스) |
| **현재 통합 폼** | `appliances` (`InputType.multiSelect`)의 선택지 중 하나로 **「에어컨」** 하나뿐. 종류 정보 없음 |
| **지금 동작** | 어댑터가 **임의로 고르지 않고** 건너뜀 + 안내: *「에어컨: 다방은 벽걸이형·스탠드형·천장형 중 하나를 고르게 되어 있는데 통합 폼에는 종류 정보가 없어 임의로 고르지 않았습니다.」* |
| **왜 임의로 고르면 안 되나** | 광고에 나가는 사실 정보이고, 틀리면 중개사가 책임집니다. 자동으로 「벽걸이형」을 찍는 것은 하면 안 됩니다 |

**제안 스펙**

```dart
// 조건부: appliances 에 '에어컨' 이 들어 있을 때만 보인다.
MasterField(
  key: 'airconType',
  label: '에어컨 종류',
  type: InputType.choice,
  options: ['벽걸이형', '스탠드형', '천장형'],
  required: false,   // 다방 「냉방 시설」 자체가 선택 항목
  example: '벽걸이형',
)
```

- **조건부 표시**: `appliances` 가 `multiSelect` 라 현재의 `visibleWhenKey` / `visibleWhenValue`
  (단일 값 일치)로는 「목록에 에어컨이 포함될 때」를 표현할 수 없습니다. 조건 모델을 늘리거나
  (`visibleWhenContains`), 무조건 보이되 안내만 다는 방법 중 하나를 골라야 합니다.
- **복수 선택 가능성**: 다방 라벨이 체크박스라 둘 이상 고를 수 있을지 모릅니다. 실물에서
  두 개를 체크해 보고, 된다면 `multiSelect` 로 가는 편이 사실에 가깝습니다. **미확인.**
- **어댑터 쪽**: `lib/remote_form.dart` 의 가전 매핑에서 `'에어컨'` 을
  `['냉방 시설', data.airconType]` 로 보내면 됩니다. 지금 그 자리에 안내를 다는 분기가 있습니다.

---

### A-2. 융자금 — 시세 대비 30% 이상/미만

| | |
|---|---|
| **다방 컨트롤** | `trade_info` / **「융자금 여부」** 행. `<select>`: `선택` · **`시세대비30% 이상`** · **`시세대비30% 미만`** · `없음` |
| **현재 통합 폼** | `loan` (`InputType.loan`) + `loanAmount` — **금액(만원)** 만 받음 |
| **지금 동작** | 「없음」이면 넣고, 금액만 있으면 구간을 정할 수 없어 건너뜀 + 안내 |

**문제의 본질**: 금액만으로는 구간을 계산할 수 없습니다. **시세**가 있어야 하는데 통합 폼에
시세 칸이 없고, 넣는다 해도 「시세」의 정의(공시가·실거래가·감정가)가 갈립니다.

**제안 — 둘 중 하나**

1. **구간을 직접 묻는다** (권장, 단순함)
   ```dart
   MasterField(
     key: 'loanRatio',
     label: '융자금 비율 (시세 대비)',
     type: InputType.choice,
     options: ['30% 미만', '30% 이상'],
     required: false,
     visibleWhenKey: 'loan', visibleWhenValue: '있음',
   )
   ```
2. **시세를 받아 계산한다** — 칸이 하나 더 늘고 정의 논쟁이 붙습니다. 직방·당근도 같은 구간을
   요구하는지 먼저 확인한 뒤 결정하는 편이 낫습니다.

> 다른 플랫폼 확인 필요: 직방·당근이 융자금을 **금액**으로 받는다면 두 칸(금액 + 구간)을 다
> 유지해야 합니다. 이 문서는 다방만 다룹니다.

---

### A-3. 단기 매물 계약기간 — 개월 수 + 협의 여부

| | |
|---|---|
| **다방 컨트롤** | 거래 종류를 **월세/전세**로 고른 뒤 나타나는 단기 관련 칸. 개월 수와 협의 여부를 **함께** 요구 |
| **현재 통합 폼** | `trade` 선택지에 `'단기'`, `shortTerm` (`InputType.toggle`) — **기간 정보 없음** |
| **지금 동작** | 건너뜀 + 안내: *「단기 매물 계약기간: 다방은 개월 수와 협의 여부를 함께 요구하지만 통합 폼에 기간 정보가 없어 화면에서 직접 골라 주셔야 합니다.」* |

**제안 스펙**

```dart
MasterField(
  key: 'shortTermMonths',
  label: '단기 계약 기간 (개월)',
  type: InputType.number,
  required: false,
  visibleWhenKey: 'shortTerm', visibleWhenValue: 'true',
),
MasterField(
  key: 'shortTermNegotiable',
  label: '단기 기간 협의 가능',
  type: InputType.toggle,
  required: false,
  visibleWhenKey: 'shortTerm', visibleWhenValue: 'true',
)
```

- **미확인**: 다방이 개월 수를 자유 입력으로 받는지 정해진 선택지(1·3·6·12개월 등)로 받는지
  채집하지 못했습니다. 거래 종류를 월세로 고른 뒤 다시 재야 합니다 — 아래 「다시 재는 법」 참고.
- `trade == '단기'` 와 `shortTerm == true` 가 **둘 다** 있어 의미가 겹칩니다. 통합 폼을 손볼 때
  이 중복도 같이 정리하는 것이 좋겠습니다.

---

### A-4. 관리비 총액 — 「기타 부과」일 때도 필요함

| | |
|---|---|
| **다방 컨트롤** | 「관리비 있음」을 고르면 **「월 관리비 상세입력」 창**이 뜹니다. 탭: `정액관리비` · `기타부과` · `확인불가`. **`확인불가` 를 뺀 두 탭 모두 총액이 있어야 「확인」 버튼이 켜집니다** |
| **현재 통합 폼** | `managementFee` (총 관리비 금액, 만원) 가 `visibleWhenKey: 'manageMethod', visibleWhenValue: '정액 관리비'` — **「기타 부과」에서는 화면에 나오지도 않아 늘 비어 있습니다** |
| **지금 동작** | 총액이 없으면 **상세입력 창을 저장하지 못하고 닫습니다** → 관리비가 통째로 안 들어갑니다 + 안내 |

**이건 새 칸이 아니라 조건 수정입니다.** `managementFee` 를 `정액 관리비` **와** `기타 부과`
양쪽에서 보이고 필수가 되게 해야 합니다.

**걸림돌**: 현재 `MasterField` 의 조건부 표시는 `visibleWhenKey` + `visibleWhenValue` **단일 값**
일치입니다. 두 값에 걸리게 하려면 모델을 늘려야 합니다 (예: `visibleWhenValues: [...]`).
이 변경은 다른 조건부 필드에도 영향을 주므로 통합 폼 작업 때 함께 하는 것이 맞습니다.

**함께 확인할 것 (미확인)**: `manageBasis`(부과 기준)와 `manageIncludes`(포함 항목)도 지금은
`정액 관리비` 에서만 보입니다. 다방의 `기타부과` 탭이 이 둘을 받는지 채집하지 못했습니다.

---

## [B] 코드만 고치면 되는 것 — 통합 폼 변경 불필요

> 이 항목들은 통합 폼에 값이 **이미 있는데** 어댑터가 안 쓰고 있습니다. 통합 폼 작업을 기다릴
> 이유가 없으니 따로 처리하면 됩니다.

### B-1. 층수 비공개 시 저/중/고 표기 구간

- **다방 컨트롤**: `additional_info` / 「층 수」 행. 「저/중/고 표기를 원할 경우 선택」 체크박스와
  함께 `<select>`: `선택` · `저` · `중` · `고`
- **통합 폼에 이미 있음**: `HifiField.floorBand` — 선택지 `['저층', '중층', '고층']`
  (`floorBandOptions`, `lib/fields.dart`)
- **지금 동작**: 어댑터가 체크박스는 켜지만 `<select>` 는 비워 두고 안내만 답니다
  (*「표기 구간은 화면에서 직접 골라 주세요」*, `lib/remote_form.dart` 의 ⑭ 층 처리)
- **고치는 법**: 체크박스를 켠 뒤 `floorBand` 를 `저층→저` · `중층→중` · `고층→고` 로 매핑해
  `select` 에 넣으면 됩니다.

---

## [C] 다방이 못 받는 것 — 안내로만 남길 것

통합 폼을 바꿔도 다방에는 들어갈 자리가 없습니다. **여기에 맞추려고 통합 폼을 깎지 마세요** —
다른 두 플랫폼이 받을 수 있습니다.

| 통합 폼 값 | 다방 사정 |
|---|---|
| `parkingPerHousehold` 세대당 주차 대수 | 다방은 **총 주차 대수**만 받음 |
| `petAllowed` 의 `불가능` · `확인 필요` | 다방은 「반려동물」 **허용 체크 하나**뿐. 셋을 구분해 저장할 수 없음 |
| 비목별 실비·정액 내역 | 다방은 **포함 항목 체크**만 받고 비목별 부과 방식은 저장하지 않음 |
| 확인 불가 법정 사유 | 상세입력 창의 「확인불가」 탭에 **사유를 고르는 칸이 없음** |
| `violation` 위반건축물 여부 | 별도 입력란 없음 → **상세 설명에 글로** 표시해야 함 |
| `loanAvailable` 대출 가능 여부 | 대응 입력란 없음 |
| `ownerPhone` 집주인 연락처 | 대응 입력란 없음 |
| `directionBase` 의 `주실 기준` | 다방은 **안방/거실**만. 어댑터가 「거실」로 넣고 안내 (공인중개사법의 주실 = 거실이나 안방) |
| `moveInType` 의 `협의 가능` | 다방은 **즉시 입주/일자 선택 중 하나를 고른 뒤** 「협의 가능할 경우」를 덧붙이는 구조. 어댑터가 즉시 입주 + 협의 가능으로 넣고 안내 |
| `floorPrivate` 완전 비공개 | 다방은 해당 층을 **반드시** 고르게 되어 있어 완전 비공개 불가 (B-1 의 저/중/고 표기가 최선) |
| LH 전세임대 여부 | 거래 종류에 따라 다방 폼에 **행 자체가 나타나지 않음** |

이미 잘 되는 것(참고): 복층형 원룸은 「복층 여부」 행으로 정확히 들어갑니다. 매물유형·주소·
면적·건축물용도·방 수·거래 종류·가격·난방/생활/보안/기타 시설·제목·상세설명·비공개 메모는
실물에서 확인했습니다 (`입력 43건 · 검증 41건`).

---

## 다른 플랫폼과 함께 볼 때

1. **A-1 에어컨 종류**는 다방만의 요구일 가능성이 높습니다. 직방·당근이 「에어컨」 유무만
   받는다면, 통합 폼에 종류를 추가해도 그 둘은 무시하면 그만입니다 — **추가해서 손해 없음.**
2. **A-2 융자금 구간**은 직방·당근이 금액으로 받는지 먼저 보세요. 다르면 금액과 구간을 **둘 다**
   두고 플랫폼별로 골라 쓰는 편이 안전합니다.
3. **A-4 조건부 표시 모델**(`visibleWhenValues`)은 다방만의 문제가 아닙니다. 조건부 필드가
   늘어날수록 단일 값 일치로는 부족해집니다. 통합 폼 작업의 **첫 단추**로 삼을 만합니다.
4. 당근 어댑터에도 같은 성격의 빈칸이 이미 있습니다(총 관리비·부과 기준·관리비에 포함이 당근에서
   **필수**인데 통합 폼 값이 없어 비워 둠). 관리비는 **세 플랫폼을 함께** 설계하는 것이 좋습니다.

---

## 다시 재는 법

계정을 넘길 필요 없이 구조만 떠 옵니다. 자세한 것은 각 파일 머리말과 README 의
「실물 계정으로 재 볼 때」를 보세요.

```
1. 크롬에서 pro.dabangapp.com 로그인 → /form/room
2. DevTools 콘솔에 test/tools/dabang_form_probe.js 붙여넣기
3. copy(__dabangProbe) 또는 파일로 저장
4. test/tools/dabang_form_probe.mirror.json 과 견주기
```

**이 문서의 미확인 항목을 채우려면**, 위 2번 **전에** 폼에서 조건을 만들어 두세요. 폼은 앞 선택에
따라 행을 다시 그리므로, 그 상태에서 떠야 그 칸이 담깁니다.

| 채우려는 것 | 먼저 할 것 |
|---|---|
| A-3 단기 계약기간의 입력 형태 | 거래 종류를 **월세**로 고르고 단기를 켠다 |
| A-4 「기타부과」 탭이 받는 항목 | 관리비 **있음** → 상세입력 창 → **기타부과** 탭 |
| A-1 냉방 시설 복수 선택 가능 여부 | 「냉방 시설」에서 두 개를 체크해 본다 |

미러 쪽 기준선은 계정 없이 언제든 다시 뜹니다:

```bash
node test/tools/mirror_layout_probe.mjs --job=structure \
  --login='https://mirror-dimension-lab.pages.dev/dabang/login/' \
  --dashboard='https://mirror-dimension-lab.pages.dev/dabang/' \
  --form='https://mirror-dimension-lab.pages.dev/dabang/form/room/' \
  --script=test/tools/dabang_form_probe.js --width=1280 --height=900
```

`--job=inject` 는 어댑터를 미러에 실제로 돌려 무엇이 들어가는지 보여 줍니다. 통합 폼을 고친 뒤
회귀를 보는 데 씁니다.
